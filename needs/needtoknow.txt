1 Add more information to Yii::app()->user
class: YumuserIdentiy 
function: credentialsConfirmed
adding sample: $this->setState('avatar', $user->avatar); 

2.temporaly change
class: YumFriendship
function: tableName
//$this->_tableName = Yum::module('friendship')->friendshipTable

3. Two forward slashes in a url (//)
This is of particular use when elements (such as the JS file in your example) could be living inside either a http or a https context. By using the double slashes, you can avoid implementing