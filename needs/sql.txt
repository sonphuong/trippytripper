UPDATE 
  ride 
SET
  del_flg = 0 
WHERE id = 43;

UPDATE 
  ride_user 
SET
  del_flg = 0
WHERE ride_id = 43;


SELECT * FROM ride;
SELECT * FROM ride_user;
SELECT * FROM message;

INSERT INTO message (
  `timestamp`,`from_user_id`,`to_user_id`,`title`,`message`
) 
VALUES
  (
    '123',
    '1',
    '2',
    'tit',
    'Sorry this trip has removed by the owner'
  ) ;

